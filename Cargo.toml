[package]
name = "bias"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Content-addressed, exhaustive decision trees — observe, decide, act"

[dependencies]
sha2 = "0.10"
hex = "0.4"
